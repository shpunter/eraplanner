# URL of the microfrontend remote's Module Federation entry.
# Point this at the deployed remote's `remoteEntry.js`, e.g.
#   VITE_LAW_REMOTE_ENTRY=https://law.example.com/remoteEntry.js
# Leave unset to render the law tab's "not connected yet" fallback.
VITE_LAW_REMOTE_ENTRY=
