# URL of the microfrontend remote's Module Federation entry.
# Point this at the deployed remote's `remoteEntry.js`, e.g.
#   VITE_MICRO_REMOTE_ENTRY=https://micro.example.com/remoteEntry.js
# Leave unset to render the /micro page's "not connected yet" fallback.
VITE_MICRO_REMOTE_ENTRY=
